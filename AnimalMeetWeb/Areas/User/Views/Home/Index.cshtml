@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome To Animal Meet Application</h1>
    <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a></p>
</div>
@*@model AnimalMeetWeb.Models.ViewModel.IndexVM


    <div class="container">
        <div class="row pb-4 backgroundWhite">
            @foreach (var animalType in Model.AnimalTypeList)
            {
                <div class="container backgroundWhite pb-4">
                    <div class="card border">
                        <div class="card-header bg-dark text-light ml-0 row container">
                            <div class="col-12 col-md-6">
                                <h1 class="text-warning">@animalType.Name</h1>
                            </div>
                        </div>
                        <div class="card-body">
                            <div class="container rounded p-2">
                                <div class="row">
                                    <div class="col-12 col-lg-8">
                                        <div class="row">
                                            <div class="col-12">
                                                @if (Model.AnimalSubTypeList.Where(u => u.AnimalTypeId == animalType.Id).Count() > 0)
                                                {
                                                    <table class="table table-striped" style="border:1px solid #808080 ">
                                                        <tr class="table-secondary">
                                                            <th>
                                                                Animal SubType
                                                            </th>
                                                            <th>Name</th>
                                                        </tr>
                                                        @foreach (var animalSubTypes in Model.AnimalSubTypeList.Where(u => u.AnimalTypeId == animalType.Id))
                                                        {
                                                            <tr>
                                                                <td>@animalSubTypes.Name</td>
                                                            </tr>
                                                        }
                                                    </table>
                                                }
                                                else
                                                {
                                                    <p>No animal subtype exist...</p>
                                                }
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>*@